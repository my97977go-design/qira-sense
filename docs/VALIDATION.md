# v7 验证记录

15 项自动测试通过；生产构建成功。排行榜完成真实游戏结算、自动弹窗、昵称上传、独立浏览器读取、上次得分、暂停与关闭检查。375/768/1024 页面无横向溢出。第二章依赖第一章，第三章依赖第二章；缺少新曲目时仍不可游玩。详见 RANKING.md。

以下为历史版本记录。

# v6 验证记录 · 2026-09-21

- 13 项自动测试全部通过；节奏目标改为每两拍一次（默认 53 次），不同 BPM 下均保持 120/BPM 秒间距。
- 课程测试覆盖任意顺序完成第一章三关、刷新保留独立进度、三关全部完成才解锁第二/第三章。
- 生产构建通过。浏览器验证三关均可直接进入，默认地图播放跨越 1—2 秒上滑和 2—6 秒揉弦边界，不停止，实时音高持续生成。
- 倒计时验证背景渐变各色均不透明，圆形宽高一致，底层数字隐藏；已检查桌面截图。
- 首页 375/768/1024 宽度无横向溢出，恢复 Qira Sense 品牌。章节在初始状态锁定，第一章全完成后显示解锁但仍明确新曲目待接入。
- 浏览器无运行错误。

以下为上一版历史验证，解锁方式与节奏密度以 v6 为准。

---

# v5 验证记录 · 2026-09-21

`npm test`：13 项通过。保留 v4 的 10 项时间、标注、BPM 和播放时钟测试，新增已知频率/谐波/静音/噪声的音高估计、技法资料关联和导入校验、课程前置条件与听感通关规则测试。

`npm run build`：生产构建通过，音高和节拍各自使用独立 Worker。

## 三关完整浏览器演练

Windows Edge / Playwright，1440×1000。全新浏览器初始仅开放第一关；按真实 AudioContext 时钟触发游戏输入，不跳过音乐、不修改应用计分。

- 第一关：完整播放 106 拍，100% 命中，解锁第二关。
- 第二关：三个实际原声片段分别得到 17、71、51 个有效音高点。一次故意错误选择可重试，三段全部答对后解锁第三关。重玩重新回到第一段，答案状态被清空。
- 第三关：完整播放 102 个连续音符，最多同时出现 9 个音符，100% 命中。未填写听感时不能完成本章；填写并保存后显示三关完成，刷新仍保持完成状态。
- 整个流程没有浏览器运行错误。

## 技法维护与声音地图

编辑大上滑解释、新增第九条测试技法并关联真实区间、导出 JSON、刷新保留修改、导入备份、拒绝错误 JSON 均通过。测试在临时浏览器环境进行，交付的默认资料库仍为 8 条教学数据。

自定义条目点击例证后显示其对应解释与动画；原声在区间末尾停止，曲线保留。修复片段结束时播放源释放可能导致曲线只剩最后一点的时序问题：分析采样同时检查真实播放状态与有效音源。

桌面与手机布局检查无横向溢出；375×667 的四轨触屏按键保持可见。教师编辑页面可在手机上纵向滚动使用。

## 最终生产包检查

直接运行 dist 的本地服务器，连续重播 1—2 秒片段五次，分别保留 17、17、17、18、17 个音高点；BPM Worker 估算 165.5 正常。所有音频、字体和 Worker 资源加载成功，无失败响应或页面错误。375、768、1024 像素宽度均无横向溢出。

## 边界

自动输入验证调度与交互，不代替真人对难度、音频延迟和音乐教学效果的评估。教学解释仍需教师审订。音高是算法估计；人工标注是原始秒级区间，四连细分仍是游戏编排。当前一个录音支撑三个关卡，不是三首录音。进度与资料修订保存在当前浏览器，不提供云端同步。

以下保留 v4 核心游戏验证记录，供回溯。

---

# 验证记录 · 2026-09-21

## 自动测试

`npm test`：10 项通过。覆盖：录音 SHA-256 与声学数据身份；13 个原始时间区间；三组四连的次数与来源；谱面连续性；节奏目标间距；按轨判定与重复输入；漏拍后计分；90/120/165 BPM 合成信号检测与静音拒绝；导入校验；倒计时暂停恢复；旧音频结束回调隔离。

`npm run build`：生产构建通过。运行时无后端、无外部字体请求。

## 浏览器实测

Windows Edge / Playwright，桌面 1440×1000、1440×900，手机布局 390×844、375×667。

- 首页大模型点击交互和提示变化正常。
- 四个页面没有横向溢出，所有测试均无 pageerror。
- 完整播放 38.88 秒挑战：故意漏掉第一拍，其余 101 个音符按音频时钟输入，并发送 repeat 事件验证长按不会重复结算。
- 实测结果：137,000 分，99% 命中，101 最高连击，101 PERFECT，1 MISS。漏拍时音乐和输入保持运行；最多同时显示 9 个可见音符。
- 倒计时内暂停后时间不变，恢复接续同一轮。
- 节奏测试自动 BPM 为 165.5，前五拍输入后得分 5,000，显示 5 / 106。
- 点击地图 32 秒、34 秒准确进入打音、回滑音区间。
- 设置中可查看 13 行原始标注。
- 375×667 布局的四个触屏按键位于可见屏幕内。

## 范围

测试证明时间调度、连续输入与界面功能，不等同于音乐专家验证全曲 BPM。人工表格提供秒级区间，四连的具体细分是游戏编排。蓝牙耳机延迟、真实触屏硬件和 Safari / Firefox 尚未进行实机验证；提供输入时差补偿供设备调整。

## 生产构建补充检查

已直接运行交付 `dist` 的本地服务器：自动节拍 Worker、音频、本地字体均成功加载，无失败资源或浏览器异常。触屏事件模拟在手机布局正确命中第一个音符；手动输入 120 BPM 生效；JSON 导出、合法文件导入、错误音频身份拒绝与恢复原标注均通过。
